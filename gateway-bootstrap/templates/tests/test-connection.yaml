apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gateway-bootstrap.fullname" . }}-test-connection"
  labels:
    {{- include "gateway-bootstrap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gateway-bootstrap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
